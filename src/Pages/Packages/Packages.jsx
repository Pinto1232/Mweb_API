import React from 'react';
import stylepack from '../Packages/Packages.module.css'

const Packages = () => {
    return (
        <div>
            Package
        </div>
    );
};

export default Packages;